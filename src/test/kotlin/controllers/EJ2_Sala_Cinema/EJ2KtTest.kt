package controllers.EJ2_Sala_Cinema

import org.junit.jupiter.api.Test

import org.junit.jupiter.api.Assertions.*
 class EJ2KtTest {

@Test
 fun manejarOpciones() {}
}

/*
* Las funciones de este programa no devuelven ningún valor (solo realizan acciones), por lo que
* esto hace que en Kotlin no sea posible hacer el plan de pruebas.
*
*Este programa se puede comprobar perfectamente ejecutándolo y ver que realmente funciona.
* Aunque alguna función como la de inicializar la sala devuelva un valor, esta no tiene
* ningún parámetro, con lo cual no se vería afectada en ningún caso.
*
*
* */